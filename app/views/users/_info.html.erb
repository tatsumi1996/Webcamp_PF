<table class='table'>
  <tr>
  <th><p>プロフィール画像</p></th>
  <th><%= attachment_image_tag(user, :profile_image, :fill, 100, 100, fallback: "no-image-icon.jpg") %></th>
  </tr>
  <tr>
  <th><p>お気に入り球団</p></th>  
  <th><%= attachment_image_tag(user.favorite_team, :image, :fill, 100, 100, fallback: "no_image.jpg") %></th>
  </tr>
  <tr>
	  <th>なまえ</th>
	  <th><%= simple_format user.nickname %></th>
  </tr>
  <tr>
	  <th>自己紹介</th>
	  <th><%= simple_format user.introduction %></th>
  </tr>
</table>

<div class='row'>
  <%= link_to '',edit_user_path(user),class: "btn btn-outline-secondary btn-block fas fa-user-cog edit_user_#{user.id}" %>
</div>